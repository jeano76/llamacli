- [22:29] DIRECTIVE received: docs/cmd-gate-double-nesting-directive.md. Task — cmd_gate() line ~622 double-nests _make_question()'s return (which is already {qid:question}) into {qid:{qid:...}} producing {"decision":{"decision":{...}}} -> would HTTP 422 on /v1/systemone. Steps: (1) fix the nesting, (2) retest via REAL cmd_gate CLI path (NOT _measure_short_circuit.py which bypasses it), confirm no 422, (3) commit.
