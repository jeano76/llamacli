# llamacli — 로컬 llama.cpp 기반 AI 코딩 에이전트 CLI 구현 프롬프트

## 0. 배경 / 목적

8GB RAM 개발용 리눅스 PC에서 동작하는, 프로그래밍·툴링 능력이 우수한 AI 코딩 에이전트
CLI를 구현한다. 이 프로그램은 다음 세 가지의 장점을 하나로 통합한다:

- **Cline**: 파일 편집/도구 실행 중심의 에이전틱 코딩 루프
- **OpenCode CLI**: 터미널 UX, 세션/모델 관리
- **Hermes**: 자가 치유(self-healing) 및 자가 개선(self-improvement) 루프

메모리(8GB) 제약이 있는 로컬 환경에서 장시간·장문맥 작업을 안정적으로 수행하는 것이
핵심 목표다. 특히 **컨텍스트가 길어지며 이전 지시나 진행 상황을 잊어버리는 문제**를
구조적으로 방지해야 한다.

## 1. 모델 백엔드

- 로컬 `llama.cpp` 서버(`llama-server`)를 **직접** 호출하는 클라이언트를 구현한다
  (subprocess spawn 또는 이미 떠 있는 서버에 HTTP 접속, 둘 다 지원).
- **OpenAI Chat Completions API 프로토콜과 완전 호환**되는 클라이언트 레이어를 둔다.
  - `/v1/chat/completions` (스트리밍 SSE 포함), `/v1/completions`, `/v1/models`,
    tool/function calling 스펙(`tools`, `tool_choice`, `tool_calls` 응답 파싱) 지원.
  - 이 레이어를 통해 llama.cpp 외의 OpenAI 호환 엔드포인트(vLLM, LM Studio, 원격
    OpenAI 계정 등)도 설정만으로 교체 가능해야 한다.
- 모델 설정(컨텍스트 길이, 스레드 수, GPU 레이어 오프로드 등)은 설정 파일로 관리하고,
  8GB 메모리 환경을 기본 프로파일로 가정한 안전한 기본값(과도한 컨텍스트 길이로 인한
  OOM 방지)을 제공한다.

## 2. 메모리 컴팩션 & 작업 연속성 (핵심 요구사항)

이 기능이 이 프로젝트의 차별점이므로 가장 엄격하게 설계한다.

### 2.1 컴팩션 트리거
- 컨텍스트 사용량이 임계치(예: 모델 컨텍스트 윈도우의 80~90%)에 도달하면 자동으로
  컴팩션을 시작한다. 수동 트리거(`/compact` 슬래시 명령)도 지원한다.

### 2.2 컴팩션 전 — 체크포인트 기록
컴팩션을 시작하기 **직전**에 반드시 다음을 로컬 파일(예: `.llamacli/state/checkpoint.json`
또는 세션별 디렉토리)에 기록한다:
- 현재 진행 중이던 작업의 목표(원래 사용자 요청 요약)
- 지금까지 완료한 단계 목록, 진행 중이던 단계, 남은 단계(TODO 리스트 형태)
- 마지막으로 수정/조회한 파일 경로들과 각각의 상태(수정 완료/진행 중/미시작)
- 마지막으로 실행하려던 도구 호출(및 그 이유) — 컴팩션 때문에 중단된 경우 복원 가능하도록
- 컴팩션 사유(자동 임계치 도달 / 수동 요청)와 타임스탬프

이 기록은 압축된 대화 요약과는 별개의, **기계가 바로 파싱해서 이어받을 수 있는 구조화된
상태 파일**이어야 한다 (사람이 읽는 대화 요약 텍스트와 혼동하지 않는다).

### 2.3 컴팩션 수행
- 오래된 대화 턴을 요약하여 컨텍스트를 축소하되, 위 체크포인트 파일에 기록된 내용은
  요약 손실과 무관하게 원본 그대로 별도 보존한다.
- 요약 시 도구 호출 결과의 원문(diff, 파일 전문 등)은 과감히 압축하되, 결정/합의된
  사항, 사용자가 명시적으로 지시한 제약조건은 반드시 요약에 보존한다(손실 금지 항목 목록화).

### 2.4 컴팩션 후 — 자동 이어하기
- 컴팩션이 완료되면 체크포인트 파일을 다시 읽어, **사용자 재입력 없이** 중단된 지점부터
  자동으로 작업을 재개한다.
- 재개 시 먼저 "무엇을 하고 있었는지"를 1~2문장으로 사용자에게 알린 뒤 이어서 진행한다
  (조용히 이어가지 않는다 — 사용자가 현재 상태를 알 수 있어야 함).
- 재개한 작업이 이미 완료되었는지(파일 상태 재확인 등) 검증 후 진행한다 — 체크포인트를
  맹신하지 않는다.

### 2.5 컨텍스트 배터리 게이지와의 연동
- 컨텍스트 사용률은 하단 상태바의 배터리 게이지에 실시간 반영되어, 사용자가 컴팩션
  임박을 미리 인지할 수 있게 한다 (§6 참고).

## 3. 헤르메스 — 자가 치유 / 자가 개선

- 도구 실행 실패(빌드 에러, 테스트 실패, 런타임 예외 등)를 감지하면 원인을 자체 분석하고
  재시도 전략을 스스로 조정하는 루프를 둔다. 단, **무한루프 방지 장치**를 필수로 포함한다:
  - 최근 N회의 도구 호출 시퀀스를 비교해 동일 패턴이 반복되면(circuit breaker) 강제
    중단하고 사용자에게 보고한다.
  - 하드 타임아웃을 둔다.
  - (참고: 사용자의 CLINE 위임 규칙에 이미 유사한 회로차단기 사고 사례와 구현 패턴이
    문서화되어 있음 — `~/bin/cline-delegate.sh`의 루프 감지 로직을 설계 참고 자료로 삼는다.)
- 자가 개선: 반복적으로 실패하거나 비효율적이었던 패턴을 세션 종료 시점에 로그로 축적하고,
  사용자 승인 하에 내부 규칙(§5의 rule)이나 프롬프트 전략을 갱신 제안한다. 자동으로
  스스로의 시스템 프롬프트나 rule 파일을 사용자 확인 없이 덮어쓰지 않는다(파괴적 행위 금지).

## 4. 아키텍처/개발 기본기 내장

이 CLI 자체가 "우수한 아키텍처 개발자"의 기본 스킬을 내장한 채로 동작해야 한다. 즉,
외부 skill 문서 없이도 기본값으로:
- 계획 우선(diff 최소화, 불필요한 추상화 금지, 기존 컨벤션 준수)
- 변경 전 관련 코드/테스트 탐색, 변경 후 검증(테스트 실행/타입체크) 습관
- 파괴적 명령(삭제, force-push 등) 전 확인
- 보안 기본기(입력 검증, 시크릿 하드코딩 금지 등)
을 시스템 프롬프트 레벨에서 내장한다.

## 5. Skill / Rule 지원

- 기존 CLI 도구들과 호환되는 `skill`(작업별 지침 묶음, 트리거 조건 기반 로딩)과
  `rule`(항상 적용되는 프로젝트/전역 규칙, 예: `.llamacli/rules/*.md` 또는
  `.clinerules` 호환) 개념을 지원한다.
- 프로젝트 루트에서 rule 파일을 자동 탐색해 세션 시작 시 시스템 프롬프트에 주입한다
  (일부 로컬 모델은 이를 자동으로 읽지 않으므로, 명시적으로 프롬프트 앞단에 강제
  주입하는 방식을 취한다).
- skill은 이름 + 트리거 설명을 가진 인덱스를 먼저 로드하고, 실제 내용은 매칭 시에만
  지연 로딩한다(컨텍스트 절약).

## 6. UX / TUI 요구사항 (Claude Code 스타일)

- 화면 하단 고정 프롬프트 입력 영역. 그 위/아래로 터미널 리사이즈에도 잔상·깨짐 없는
  ANSI 구분선을 유지한다(alternate screen buffer 또는 정밀한 커서 위치 재계산 필요).
- **하단 고정 상태바**에 다음을 항상 표시:
  - 현재 작업 디렉토리
  - 현재 사용 중인 모델명
  - 컨텍스트 사용량을 나타내는 작은 막대(bar) 형태의 배터리 게이지 애니메이션
- 에이전트 동작 중에는 프롬프트 앞에 문자 스피너(예: 브레일 패턴 스피너)를 표시해
  작업 진행 중임을 나타낸다.
- `/` 입력 시 팝업 메뉴 제공 (종료, help, 메시지 큐 입력 등 탐색 가능):
  - 팝업은 화면의 다른 그래픽 요소를 침범/훼손하지 않고 오버레이되어야 한다.
  - 팝업이 닫히면 이전 셸 화면 상태가 정확히 복구되어야 한다(underlying buffer 보존
    후 복원, 리페인트 시 잔상 없음).
- 터미널 리사이즈 이벤트(SIGWINCH)에 대응해 레이아웃을 재계산하고 깜빡임 없이
  다시 그린다.

## 7. 비기능 요구사항

- 8GB RAM 환경을 기준으로 메모리 사용량을 프로파일링하고, CLI 프로세스 자체의
  상주 메모리를 최소화한다(모델 메모리는 별도 llama.cpp 프로세스가 사용).
- 로컬 모델 사용을 기본으로 하되, OpenAI 호환 원격 엔드포인트로도 설정 전환 가능.
- 세션 상태(체크포인트, 대화 로그, rule/skill 캐시)는 프로젝트별 `.llamacli/` 디렉토리에
  저장한다.

## 8. 구현 우선순위 (제안 → 진행 상황 반영, 2026-09-17 실기 검증 기준)

1. ✅ llama.cpp 직접 호출 + OpenAI 호환 클라이언트 레이어
   — 실제 프로덕션 `llama-server`(Ornith-1.5-35B, tool-calling 지원)에 붙여 `/v1/chat/completions`
   스트리밍·`tool_calls` 파싱까지 실기 검증 완료.
2. ✅ 기본 에이전트 루프(도구 호출: 파일 read/write/edit, 셸 실행)
   — `write_file`/`edit_file` 실행 후 실제 파일이 생성/수정되는 것까지 확인.
3. ✅ 컴팩션 전/후 체크포인트 기록 및 자동 재개 (§2)
   — `send()`/`runUntilIdle()`과 `forceCompact()`/`compact()`가 동시 실행될 때
   `AgentLoop.messages`가 손상되는 경쟁 조건을 발견 후 `taskChain` 기반 직렬화로 수정,
   재검증 완료.
4. ✅ **변경 사항의 diff 표시 (ANSI 컬러)** — 최초 우선순위 목록에는 없었으나, 실사용
   테스트 중 파일 변경 내용을 눈으로 확인할 수 없다는 점이 발견되어 추가한 항목.
   `src/tools/diff.ts`(LCS 기반 라인 diff, 초록/빨강/회색 ANSI)로 구현하고, 신규 파일
   생성(전체 `+`)과 기존 파일 부분 수정(컨텍스트는 회색, 변경분만 색상) 양쪽 다 실제
   모델이 호출한 `write_file`/`edit_file` 결과로 실기 검증 완료. Ink `Text`가 문자열에
   박힌 raw ANSI 코드를 깨지 않고 그대로 통과시켜 재직렬화한다는 것도 함께 확인.
5. ✅ TUI (하단 입력창, 상태바, 스피너, 슬래시 팝업)
   — pty 기반 실기 구동으로 스트리밍 렌더링·상태바·`/help`·`/quit`(반전 하이라이트 →
   커서 복원)까지 확인. **알려진 테스트 하네스 함정**: pty의 winsize를 `TIOCSWINSZ`로
   명시적으로 설정하지 않으면(0x0) Ink가 콘텐츠 없이 clear-screen만 무한 반복한다 —
   실제 터미널에선 항상 유효한 크기가 잡히므로 코드 결함은 아니지만, 이 CLI를 다시
   자동화 테스트할 때 반드시 참고할 것.
6. ✅ skill/rule 로딩 — `/skills`, `/rules` 슬래시 명령으로 로드된 목록 조회까지 연결.
7. ✅ 헤르메스 자가 치유/개선 루프 (회로차단기 포함)
   — 회로차단기(반복 패턴 감지 + 하드 타임아웃)에 이어 "자가 개선 제안"(§3 두 번째 항목)도
   구현: `src/hermes/selfImprove.ts`가 실패 로그를 도구+정규화된 에러 시그니처로 그룹핑해
   같은 패턴이 2회 이상 반복될 때만 모델에게 rule 초안을 작성하게 한다. 실제 모델로 검증
   (동일 `edit_file` 실패 3회 입력 → 구체적인 예방 rule markdown 생성 확인). **절대 자동
   적용하지 않음** — `/improve`(제안만 보여줌, 파일 변경 없음) / `/improve-apply`(승인 후
   `.llamacli/rules/hermes-proposed-<timestamp>.md`로 새 파일 저장, 기존 rule은 절대
   덮어쓰지 않음)로 분리. `/quit` 시 미검토 실패 로그가 있으면 자동으로 제안을 보여주고
   한 번 더 `/quit`해야 종료되도록 세션 종료 시점 트리거도 연결.

### 8.1 남은 TODO (README "구현 상태"와 동기화)
- 체크포인트의 `pendingToolCall` 수집 — 현재는 턴 사이에서만 컴팩션을 체크하므로 항상
  `null`. 턴 도중 중단을 지원하려면 도구 실행 루프 안에서도 컴팩션 체크가 필요.
- StatusBar의 컨텍스트 게이지가 아직 `estimateTokens`(문자 수 기반 추정치)에 연결됨 —
  실제 토크나이저(llama.cpp `/tokenize`) 연동과 함께 AgentLoop → UI로 사용량 전달 필요.
- 로그의 `log.slice(-logHeight)`가 항목(entry) 개수 기준이라, 여러 줄짜리 diff 항목은
  렌더링 시 줄 단위로 펼쳐지므로 화면이 `logHeight`보다 살짝 넘칠 수 있음(사소한 표시 한계).

---
*이 문서는 구현을 지시하기 위한 명세서이며, 각 섹션은 별도 이슈/작업 단위로 분리해
진행할 수 있다. §8은 실제 구현·실기 검증이 끝날 때마다 갱신한다.*
